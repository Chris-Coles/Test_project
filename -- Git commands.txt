Git commands

git checkout -b NEW-BRANCH-NAME

is same as 

git branch NEW-BRANCH-NAME -- make a branch
git checkout NEW-BRANCH-NAME -- check it out

remember to publihs branch to tell github about it 